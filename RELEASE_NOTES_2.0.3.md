# HomeMovie v2.0.3

- 优化自定义 JSON 刮削：支持多个 JSON 刮削配置，并可选择当前使用的方案。
- 改进自定义 JSON 字段配置：支持先测试样本 JSON，再从树形节点中选择字段路径，并显示字段与测试结果的对应关系。
- 修复自定义 JSON 字段解析：发行日期支持 UTC 时间转换到北京时间后取日期；评分、Fanart、演员数组等字段提取更稳定。
- 影片详情页新增自定义 JSON 刮削入口，可直接从详情页使用当前选中的自定义 JSON 配置进行刮削或重新刮削。
- 修复“清除刮削并还原”后需要退出页面才看到效果的问题，清除后只刷新当前影片，不再触发全库扫描。
- 修复 115 网盘文件夹超过 1000 条内容时只能读取第一页的问题，现在会自动分页读取完整目录。
